language: python

#version
python:
  - "3.6"

notifications:
  slack: qed-epa:FnRva7RtXZLlczmpxShtokg

before_install:
 - wget https://github.com/quanted/requirements_qed/requirements.txt

install:
  - pip install -r https:requirements.txt

# command to run tests
script:
  - nosetests --with-cov --cov pram --cov-config .coveragerc --logging-level=INFO

after_success:
  - coveralls --config_file .coveragerc