language: python

#version
python:
  - "2.7"
  - "3.5"

# command to install dependencies
#install: "pip install -r requirements.txt"

#containerized so no sudo
install:
  - pip install --user -r requirements.txt

# command to run tests
script:
  - nosetests -sv